# Production Requirements for Render/Railway Deployment
# Optimized for fast cold starts and minimal memory usage
# Compatible with Python 3.11-3.13

# Core Framework
flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.1

# Environment & Config
python-dotenv==1.0.0

# AI & ML APIs
groq==0.4.2
pinecone>=5.0.0,<7.0.0

# HTTP client (pinned for Groq compatibility)
httpx<0.28

# Embeddings (local models) - disabled for HF API mode to reduce RAM/build size
# sentence-transformers>=2.3.0,<3.0.0
# torch>=2.5.0
# transformers>=4.36.0,<5.0.0
# fastembed>=0.3.6

# Utilities
numpy>=1.24.0,<2.0.0
huggingface_hub>=0.24.6

# Production Server
gunicorn==21.2.0
